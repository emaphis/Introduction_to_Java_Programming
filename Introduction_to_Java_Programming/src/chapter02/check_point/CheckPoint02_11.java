/*
 *  Check Point 2.11
 */
package chapter02.check_point;

/**
 *
 * @author emaph
 */
public class CheckPoint02_11 {

    public static void main(String[] args) {
        // a.
        double a = 1;
        double b = 1;
        double c = 1;
        double d = 1;
        double r = 1;
        double aa = 4.0 / (3 * (r + 34)) - 9.0 * (a + b * c) + (3 + d * (2 + a) )/ (a + b * d);

        // b.
        double t = 1;
        double bb = 5.5 * Math.pow(r + 2.5, 2.5 + t);
    }

}
